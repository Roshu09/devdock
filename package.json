{
  "name": "devdock-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "node --experimental-vm-modules node_modules/.bin/jest",
    "lint": "eslint packages/*/src/**/*.js"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "jest": "^29.0.0",
    "sharp": "^0.35.1"
  },
  "type": "module"
}
